import React, { Component } from 'react';

export class AllRecords extends Component {
   render() {
      return (
         <section>
            all records
         </section>
      );
   }
}

export default AllRecords;
